# CLAUDE.md — 맞춤 과외 매칭 프로젝트 가이드

## 프로젝트 개요
직접 가르쳐 온 선생님이 1:1 상담으로 가장 잘 맞는 과외 선생님을 엄격하게 필터링하여 매칭해 주는 웹사이트.
유일한 전환 목표는 **무료 상담 신청(리드) 수집**이며, 모바일 검색으로 진입하는 학부모를 주 타겟팅으로 삼는다.

---

## 핵심 비즈니스 규칙 및 워딩 규칙 (매우 중요)
1. **워딩 절대 규칙**: 
   - 사이트 어디에도 **"컨설턴트"**라는 단어를 절대 사용하지 않는다.
   - 반드시 **"선생님"**, **"상담 선생님"**, **"직접 가르쳐 온 선생님"**으로 통일하여 표기한다.
   - 어조 타겟: 영업·과장 배제, 온화하고 차분하면서도 "내가 오랫동안 가르쳐봤으니 안다"는 전문 교사의 강한 내공과 확신을 담은 톤앤매너.
2. **핵심 슬로건**: 
   - `"선생님을 보는 눈은, 선생님이 가장 정확합니다."`
3. **전환 목표**:
   - 모든 사용자 행동 흐름은 **"무료 상담 신청"**으로 유도한다.

---

## 디자인 토큰 & 스타일 규칙
- **메인 컬러 (Primary)**: 차분하고 신뢰성 있는 쨍한 보라색 (`#6D28D9` 계열) + 깨끗한 흰색 (`#FFFFFF`)
- **포인트 컬러 (CTA 전용)**: 따뜻함과 눈에 띄는 대비를 보여주는 따뜻한 오렌지·코랄 계열 (`#FF6B4A` 또는 `#EA580C` 계열)
- **텍스트 컬러**: 본문은 진한 차콜 (`#1F2937`), 보조 텍스트는 중간 톤 회색 (`#4B5563` 또는 `#6B7280`)
- **타이포그래피**: 
  - 서체: **Pretendard** 전면 적용
  - 헤드라인 강도: `font-bold` (`font-extrabold` 대제목용)
  - 전역 스페이싱 및 줄바꿈: `word-break: keep-all` (한국어 가독성을 위해 어절 단위 줄바꿈 유지)
- **레이아웃 제약**:
  - **모바일 필수 최적화 (390px 기준)**: 대형 디스플레이 이전에 모바일 한 손 터치 조작과 반응형 레이아웃을 최우선으로 고려한다.
  - 가로 스크롤 없음, 터치 타겟 48px 이상 확보.
- **비주얼 분위기**: 따뜻함 + 전문성과 정직함. 흐릿하거나 미숙한 느낌 배제. 과도한 그라데이션 및 과시적인 화려한 전환 애니메이션은 금지한다.

---

## 데이터 분리 원칙
- 하드코딩 절대 금지: UI 컴포넌트에 직접적인 서비스 카피, 커리큘럼 명세, 성과 후기, 지역/학년 매칭 데이터 등을 직접 입력하지 않는다.
- 모든 데이터는 확장성(pSEO 랜딩페이지 생성 등)을 고려하여 `src/data/` 내부의 전용 TypeScript 데이터셋 파일로 완전히 격리한다.

---

## 공통 제약조건 및 접근성
1. **시맨틱 마크업**: `header`, `main`, `footer`, `section` 태그 구성 및 제목 위계 준수 (페이지당 `h1`은 오직 1개).
2. **콘텐츠 구분**: 인터랙티브 요소는 링크(`<a>`, `/#consult`)와 기능적 버튼(`<button>`)을 명확히 분리하여 사용한다.
3. **접근성 수칙**: 대체 텍스트 (`alt`), 명확한 이름 매핑 (`aria-label`, `<label>`)을 확보하고, 충분한 색 대비를 보장한다.
4. **스크롤 효과**: 부드러운 스크롤 (`scroll-behavior: smooth`)을 적용하고, 불필요한 레이아웃 시프트를 예방한다.
